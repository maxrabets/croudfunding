import React from 'react';
import { Typography} from '@material-ui/core';

const Comment = ({user}) => {
    return (        
        <Typography >
            User link
        </Typography>
    );
}

export default Comment;